#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

npx lint-staged

# Type-check frontend
cd frontend && npm run typecheck || exit 1
cd ..

# Type-check backend
cd backend && npm run typecheck || exit 1
cd ..
